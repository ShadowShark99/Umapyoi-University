<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <h1> <%= title %></h1>
  <% if(locals.umamusume) { %>
    <% for(let i = 0; i < umamusume.length; i++) { %>
      <div> <%- include("./partials/uma", {uma: umamusume[i]}) %></div>
    <% } %>
    
  <% } %>
  <a href="/enroll">Enroll Your Uma</a>
  
</body>
</html>